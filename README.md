# C++ Limit Order Book + Matching Engine

Single-instrument, price–time priority limit order book and matching engine in C++ (pooled allocation, differential fuzz vs a slow reference, percentile latency benches).
